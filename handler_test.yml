---
- name: handler test
  hosts: all
  handlers:
    - name: stop message
      debug:
        msg: stop play

  tasks:
    - name: execute ls
      shell: ls
      notify:
        - stop message
